
import { Color, Scale } from 'chroma-js';


class SvgAnnotationHandler {
  //

  constructor() {
    // prepare SVG viewport
  }

  onResize() {
    // update SVG viewport;
    // if necessary apply SVG tree
  }

  async highlightSubpath(
    path: string | number,
    start: number | BigInt,
    end: number | BigInt,
    color: Color,
    width: number) {


      // use path offsets to get path step range, including
      // offsets for first & last step

      // 


    }
}
